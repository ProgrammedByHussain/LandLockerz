type NFT = record {
    id: nat64;
    owner: text;
    metadata: text;
};

service : {
    "get_nft": (nat64) -> (opt NFT) query;
    "mint_nft": (text, text) -> (nat64);
    "transfer_nft": (nat64, text) -> (bool);
};